# Variables de entorno para SukaBack
# Copia este archivo como .env y configura los valores reales

# Puerto del servidor
PORT=3000

# JWT Secrets (CAMBIAR en producción)
JWT_SECRET=change_me_super_secret_key_for_access_tokens
JWT_REFRESH_SECRET=change_me_super_secret_key_for_refresh_tokens

# JWT Timeouts (tokens de larga duración para acceso persistente)
# Access token: 30 días - el usuario no necesita reautenticarse frecuentemente
# Refresh token: 90 días - permite renovar el access token si es necesario
JWT_ACCESS_TTL=30d
JWT_REFRESH_TTL=90d

# Base de datos PostgreSQL (AWS RDS)
DATABASE_URL=postgresql://suka:<TU_PASSWORD>@sukadb.c6pq4u2yk89i.us-east-1.rds.amazonaws.com:5432/sukadb

# CORS para frontend React
# Puedes especificar múltiples orígenes separados por comas
# Ejemplos:
# - Desarrollo: http://localhost:5173
# - Producción: https://tu-frontend.com,https://www.tu-frontend.com
# - Permitir todos (no recomendado en producción): *
CORS_ORIGIN=http://localhost:5173

# Entorno de desarrollo
NODE_ENV=development